---

- name: force systemd to reread configs
  systemd:
    daemon_reload: true

- name: start and enable services
  service:
    name: "{{ item }}"
    state: started
    enabled: true
  loop:
    - redis
    - redis-sentinel

...
