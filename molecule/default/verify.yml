---

- name: Verify
  hosts: all
  become: true
  ignore_errors: true
  tasks:
    - name: Ensure redis and sentinel ports are reachable
      wait_for:
        host: localhost
        port: "{{ item }}"
        timeout: 3
      loop:
        - 6379
        - 16379

...
